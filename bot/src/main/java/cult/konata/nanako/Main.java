package cult.konata.nanako;

public class Main {
    public static void main(String[] args) {
        // We expect the passed parameter to be the bot token, so its 0.
        if (args[0] == "") {
            System.out.println("No bot token passed. Stopping.");
            System.out.println("usage: nanako [token]");
        } else {

        }
    }
}
